Role: You are a Senior Full-stack Architect specialized in ReactJS and Spring Boot.

Objective: Read, analyze, and understand the current Frontend (React) and Backend (Spring Boot) codebases to prepare for API integration. Do not write any integration code yet. Your goal is to identify patterns, data structures, and potential discrepancies.

Input Data: I will provide you with the following files (or you can read them from the context):

Backend: Controller files, DTO classes, and Entity classes (specifically for User, Product, and Order).

Frontend: package.json, key Component files (e.g., ProductList.jsx, LoginPage.jsx), and the Mock Data files currently being used.

Analysis Tasks: Please analyze the provided code and generate an "Integration Readiness Report" covering these 4 points:

Data Structure Gap Analysis:

Compare the Backend DTO JSON structure vs. the Frontend Mock Data structure.

List every field mismatch (e.g., Backend uses product_name vs. Frontend uses title).

Recommend a mapping strategy (Should we fix the Backend DTO or map it in the Frontend Service?).

State Management Review:

Identify how the Frontend currently manages state (local useState, Context API, or Redux?).

Suggest how to integrate async API calls into this existing state management without refactoring the whole app.

Endpoint Verification:

List the available API endpoints found in the Backend Controllers.

Match them with the Frontend features that need them.

Security & Auth Pattern:

Analyze the AuthController to understand the login flow (JWT, Session, or Basic Auth).

Check axios dependencies in package.json to confirm readiness.

Output:

A structured Markdown report.

A table comparing Frontend Fields vs. Backend Fields.

Wait for my confirmation before generating any code.