# Database Configuration for Local PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/shopltt_db
spring.datasource.username=postgres
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger Configuration
springdoc.swagger-ui.path=/swagger-ui.html

# Logging Configuration
logging.level.com.watchstore.backend=DEBUG
logging.level.org.springframework.web=DEBUG

# JWT Configuration
# Secret key for JWT token signing (Base64 encoded, min 256 bits)
# IMPORTANT: Change this secret in production environment!
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
# Token expiration time in milliseconds (86400000 = 24 hours)
jwt.expiration=86400000
# Server Configuration
server.port=5000